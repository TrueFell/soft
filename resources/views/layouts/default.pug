html
    head
        meta(name='csrf-token', content=csrf_token())
        title My Site
        block scripts
            script(type='text/javascript')
                include js/app.js
            script(type='text/coffeescript')
                include coffee/app.coffee
            style
                include css/app.css
    body
        block content
